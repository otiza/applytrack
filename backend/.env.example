PORT=3000
# Comma-separated allowlist (example):
# CORS_ORIGIN=https://app.example.com,https://www.app.example.com
# Use * only if you explicitly want to allow all origins.
CORS_ORIGIN=*
TRUST_PROXY=1
NODE_ENV=production
DATABASE_URL=postgresql://applytrack:applytrack@localhost:5432/applytrack?schema=public
JWT_SECRET=super-secret-change-me
JWT_EXPIRES_IN=7d
